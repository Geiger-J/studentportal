# Demo profile — activated alongside local: --spring.profiles.active=local,demo
# No simulation time here — set manually mid-recording for SC 13 demo
# Scheduler stays enabled (needed for MATCHED -> DONE transition)
